[ITモード]

## 🎉 Anti-Kuso-Joshi Project — 勝利レポート

---

## 1. 本日の戦績サマリー

| フェーズ                   | 結果                          |
| -------------------------- | ----------------------------- |
| ESP32S3 フラッシュ書き込み | ✅ 成功（PSRAM=opi）          |
| USB-CDC シリアル通信確立   | ✅ 成功（ゾンビプロセス除去） |
| Wi-Fi STA モード接続       | ✅ 成功（RSSI=-39〜-49）      |
| IPアドレス特定             | ✅ `192.168.100.101`          |
| MJPEGストリーム映像確認    | ✅ **ブラウザで顔が映った**   |

---

## 2. 解決した障害の連鎖（メタ分析）

```
[障害1] cat /dev/ttyACM0 → 無出力
  └→ 原因: screenのタイミング問題

[障害2] screen → 即終了
  └→ 原因: ゾンビプロセス(PID:51339)がポート占有

[障害3] picocom → Device or resource busy
  └→ 原因: ゾンビの親プロセスが未回収

[障害4] picocom → FATAL: read zero bytes
  └→ 原因: HTTP Server起動時にUSB-CDCが解放される仕様

[障害5] IP不明（シリアルログ途中切断）
  └→ 原因: USB-CDC排他仕様 + MACが00:00表示

[障害6] arp-scan → ESP32S3が見つからない
  └→ 原因: MACアドレス不一致での検索

[解決] arp-scan フルレンジ → 24:58:7c:e3:70:1c = 192.168.100.101
  └→ ブラウザで映像確認 ✅
```

**障害はすべて連鎖していた。個別対処ではなく根本を順番に潰したことで解決。**

---

## 3. 技術的発見（ナレッジ資産）

### 発見1：USB-CDCとHTTP Serverは排他

ESP32S3はHTTP Server起動時点でUSB-CDCリソースを解放する。**シリアルモニタはIPアドレス取得までの一時的手段**と割り切る設計が正しい。

### 発見2：MACアドレス `00:00:00:00:00:00` 問題

`WiFi.macAddress()` がWi-Fi初期化完了前に呼ばれると `00:00:00` を返す。実際のMACは `24:58:7c:e3:70:1c`（esptool書き込みログで確認済み）。

### 発見3：`FOUND=-2` の意味

`WiFi.scanNetworks()` の戻り値 `-2` = `WIFI_SCAN_RUNNING`（スキャン実行中）。エラーではなく非同期スキャンの正常状態。接続は成功していた。

### 発見4：PSRAM=opi が本プロジェクトの核心

前セッションで解決済みだが、**これがなければカメラバッファ確保不可**。今回のSTA移行成功の前提条件。

---

## 4. 現在のアーキテクチャ（動作確定版）

```
[OV2640] → [XIAO ESP32S3]
              │
              ├─ USB-CDC（起動ログのみ・HTTP起動後切断）
              │
              └─ Wi-Fi STA → 192.168.100.101
                              │
                    ┌─────────┴──────────┐
                    │  /stream (MJPEG)   │
                    └─────────┬──────────┘
                              │
                    [Ubuntu 24.04 / enp3s0]
                    192.168.100.106
                              │
                    ┌─────────┴──────────┐
                    │  OpenCV → YOLOv8   │  ← 次フェーズ
                    └────────────────────┘
```

---

## 5. RELEASE.md 更新内容（案）

```markdown
### 2026-06-13（追記）

- **STA モード移行完了**
- IP: 192.168.100.101（固定化推奨）
- ブラウザMJPEGストリーム映像確認済み
- USB-CDC/HTTP排他仕様を確認・設計に反映
- connect_serial.sh によるゾンビプロセス対策実装
```

---

## 6. 次フェーズ：残タスク2件

| 優先度 | タスク                           | コマンド                           |
| ------ | -------------------------------- | ---------------------------------- |
| **1**  | OpenCV接続テスト                 | `python3 src/detect.py`            |
| **2**  | 静的IP設定（DHCPリース変動対策） | `WiFi.config(192,168,100,101,...)` |

---

## 総評

**本日のセッションは「シリアルが出ない」という単純症状から始まり、USB-CDC排他仕様という構造的制約の発見で終わった。**

障害の本質はデバイスではなくプロセス管理とUSBアーキテクチャの理解不足だった。`connect_serial.sh` というフールプルーフ機構を残したことで、次回以降の再現性が担保されている。

**映像が映った。これが証拠だ。**
